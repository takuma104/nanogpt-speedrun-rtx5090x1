# autoresearch

LLMが自分自身で研究を行うという実験です。

## 背景

このリポジトリはupstreamがNanoGPTのspeedrunです。(README.mdを参照のこと)
upstream同様、モデルやトレーニングコードを調整して、cross-entropy lossが3.28以下になるまでの`train_time`を短縮するのが目的です。upstreamはH100x8の構成で最適化を進めていますが、このブランチではRTX5090x1と、かなり違う環境で最適化を行いたいです。

## 実験

以下のコマンドでトレーニングが走ります。

```
./run.sh
```

標準出力に以下のように結果が表示されます。(Baselineでは1703秒後程度)です。

```
step:1285/1285 val_loss:3.2805 train_time:1703745ms step_avg:1325.87ms
```

ここでの`val_loss`が3.28以下程度になったときの `train_time`(この場合、1703.745秒)を採用してください。

**できること:**
- すべてのコードの編集
- 使用するデータセットも変更可能です。ただしvalidationでのcross-entropy lossの計算およびそれに使用するデータセットはbaselineから変更しないでください。
- masterブランチのコミット履歴は過去の歴戦の進化の過程が見られます。BaselineはH100x8に最適化されていたものをそのままRTX5090で動くようにしたもののため、RTX5090x1では別の方法があったかもしれません。
- インターネットアクセスは自由です。公開されているコードや論文等で手法を調査することなども可能です。
- subagentで議論するのも可能です。また別の視点が欲しい場合はcodexコマンドも使用可能ですのでheadlessで有効活用してください。
- pytorchやtritonなどのバージョンの変更も可能です。uvコマンドで更新してください。

**目標はシンプルです： モデルやトレーニングコードを調整して、cross-entropy lossが3.28以下程度になるまでの`train_time`を短縮する** 

上記の「できること」が可能です。ただし実験として意味がなくなるような報酬ハックはやめてください。

**シンプリシティ基準**: 他のすべてが等しい場合、シンプルが優れています。わずかな改善で醜い複雑さを追加するのであれば、価値がありません。逆に、何かを削除して同じか良い結果を得るのであれば、素晴らしい成果です — これは簡素化の勝利です。

**最初の実行**: 最初の実行は常にベースラインを確立するためのものであるため、コードはそのまま実行します。

## 結果ノートへの記載
`docs/research_note.md` に記述してください。
`train_time`が小さくなった場合は、「現在の最小`train_time`」を更新してください。また、`logs/`フォルダに自動的にログが生成されるので、このファイル名も記録してください。(標準出力に `logs/175e383c-fb30-4e52-bf4e-a149132fef34.txt` のような行が表示されます。)
また成功の概要、失敗の概要、気付きなどをメモしていってください。

## 実験ループ

**永遠にループする：**

1. 実験的なアイデアでコードを編集する
2. 実験を実行し、結果ノートにやったことと結果概要を追記する
3. 結果がよければコード/結果ノートをgit commitする。結果が悪ければ結果ノートのみgit commitし、コードは元に戻す。commit後、適宜 git pushしてください。
4. 1にもどる

このアイデアは、あなたが物事を試している完全に自律的な研究者であるということです。それらが機能する場合は保持します。そうでない場合は破棄します。ブランチを進めているので、反復することができます。何らかの方法で立ち往生していると感じる場合は、巻き戻すことができますが、これは非常に非常に少なくする必要があります（あればです）。

**決して停止しないでください**: 実験ループが始まった後（初期セットアップ後）、人間に続けるべきか尋ねるために一時停止しないでください。「続けるべきですか？」または「これは良い停止点ですか？」と聞かないでください。人間は寝ているか、コンピューターから離れている可能性があり、手動で停止されるまで無期限に作業を続けることを期待しています。あなたは自律的です。アイデアが尽きた場合は、もっと難しく考えてください — コード内で参照されている論文を読み、スコープ内のファイルを再読して新しい角度を見つけ、以前の近い誤りを組み合わせ、より根本的なアーキテクチャの変更を試してください。ループは人間が手動で停止するまで実行されます。
